import {graphql, StaticQuery} from "gatsby";
import Img from "gatsby-image";
import React from "react";

const LogoSlider = () => (
    renderButton()
);

const AdSlider = () => (
    renderButton()
);